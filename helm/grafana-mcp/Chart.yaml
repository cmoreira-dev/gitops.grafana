apiVersion: v2
name: grafana-mcp-wrapper
description: Wrapper chart to install grafana-mcp via Helm
type: application
version: 0.1.0

dependencies:
  - name: grafana-mcp
    version: "0.3.1"
    repository: https://grafana.github.io/helm-charts
